---
import ToolLayout from '~/layouts/ToolLayout.astro';
---

<ToolLayout
  title="URL Encoder & Decoder"
  description="Encode or decode URL strings using standard percent-encoding. Free online URL tool."
  slug="url-encoder"
  category="Developer Tools"
>
  <div class="space-y-6">
    <div class="flex gap-2 flex-wrap">
      <button id="ue-enc" class="px-5 py-2.5 bg-blue-600 text-white text-sm font-medium rounded-lg hover:bg-blue-700 transition">Encode →</button>
      <button id="ue-dec" class="px-5 py-2.5 bg-white dark:bg-slate-700 text-gray-700 dark:text-gray-200 text-sm font-medium rounded-lg border border-gray-300 dark:border-slate-600 hover:bg-gray-50 transition">← Decode</button>
      <button id="ue-copy" class="px-4 py-2.5 bg-white dark:bg-slate-700 text-gray-700 dark:text-gray-200 text-sm font-medium rounded-lg border border-gray-300 dark:border-slate-600 hover:bg-gray-50 transition ml-auto">Copy Output</button>
    </div>
    <div>
      <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Input</label>
      <textarea id="ue-in" class="w-full px-4 py-3 rounded-lg border border-gray-300 dark:border-slate-600 bg-white dark:bg-slate-900 text-gray-900 dark:text-gray-100 font-mono text-sm min-h-[150px] resize-y focus:ring-2 focus:ring-blue-500 focus:border-transparent outline-none transition" placeholder="https://example.com/path?name=hello world" spellcheck="false"></textarea>
    </div>
    <div>
      <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Output</label>
      <textarea id="ue-out" class="w-full px-4 py-3 rounded-lg border border-gray-200 dark:border-slate-700 bg-gray-50 dark:bg-slate-800 text-gray-900 dark:text-gray-100 font-mono text-sm min-h-[150px] resize-y outline-none" readonly placeholder="Result..." spellcheck="false"></textarea>
    </div>
  </div>

  <script is:inline>
    const i=document.getElementById('ue-in'),o=document.getElementById('ue-out');
    document.getElementById('ue-enc').addEventListener('click',()=>{ o.value=encodeURIComponent(i.value); });
    document.getElementById('ue-dec').addEventListener('click',()=>{ try{o.value=decodeURIComponent(i.value);}catch(e){o.value='Error: invalid encoded string';} });
    document.getElementById('ue-copy').addEventListener('click',()=>{ if(o.value)navigator.clipboard.writeText(o.value); });
  </script>
</ToolLayout>
